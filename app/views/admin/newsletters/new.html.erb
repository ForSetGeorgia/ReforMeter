<% model_class = Newsletter %>
<% page_title t('shared.actions.with_obj.new',
                obj: t('activerecord.models.newsletter', count: 1)) %>

<div class='actions-container'>
  <%= render partial: 'shared/new_actions',
        locals: {
          hide_object_name: true,
          object: Verdict.new,
          index_path: admin_newsletters_path
                }
  %>
</div>

<%= render partial: 'form' %>
